// Hook placeholder for future behavior.
// Currently the UI is static; this hook keeps the file available
// for future enhancements without breaking imports.
import { useEffect } from 'react';

export default function useBarraH() {
  useEffect(() => {
    // placeholder: no behaviour for static version
    return () => {};
  }, []);

  return {};
}